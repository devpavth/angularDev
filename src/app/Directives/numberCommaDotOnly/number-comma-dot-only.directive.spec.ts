import { NumberCommaDotOnlyDirective } from './number-comma-dot-only.directive';

describe('NumberCommaDotOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberCommaDotOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
